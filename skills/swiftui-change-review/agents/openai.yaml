interface:
  display_name: "SwiftUI Change Review"
  short_description: "Run an explicit SwiftUI semantic review"
  default_prompt: "Use $swiftui-change-review to run an explicitly requested indexed semantic review before source-diff analysis."
policy:
  allow_implicit_invocation: false
