interface:
  display_name: "SwiftUI Dataflow Refactor"
  short_description: "Run an explicit semantic data-flow refactor"
  default_prompt: "Use $swiftui-dataflow-refactor to run an explicitly requested indexed refactor of one semantic-value cluster."
policy:
  allow_implicit_invocation: false
