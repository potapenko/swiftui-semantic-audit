interface:
  display_name: "SwiftUI Semantic"
  short_description: "Run an explicit SwiftUI semantic workflow"
  default_prompt: "Use $swiftui-semantic to run an explicitly requested deterministic semantic-twin workflow."
policy:
  allow_implicit_invocation: false
