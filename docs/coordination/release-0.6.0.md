# Contract Change Envelope — RELEASE-0.6.0-001

- Task: Publish the accepted 0.6.0 project-watcher candidate through GitHub, Homebrew, the local machine, separately installed tagged skills, and truthful public release facts.
- Change mode: `Evolve` for release state and distribution; the accepted flag-ownership, diff-fallback, and persisted-slice parser repairs remain behavior-preserving realization evidence.
- Authorized by: user approval on 2026-08-23 of the concrete 0.6.0 release plan, plus the explicit decision that the social preview must contain no product version.
- Authorized domains: release status, immutable tag/archive, upstream Homebrew formula, tagged four-skill installation, public installation/reference copy, release-version website facts, and exact terminal receipts.
- Authorized clauses: `PC-OPS-012`, `PC-REL-001..005`, `PRJ-*`, `CLI-GEN-001/005`, `CLI-PRJ-001..006`, `ACC-PRJ-*`, `ACC-WATCH-*`, `ACC-DOG-004`, `ACC-SKILL-001..008`, `ACC-CI-001..004`, `ACC-BREW-001..004`, `ACC-DOD-016..017`, `WEB-DELTA-010`, `WEB-INSTALL-001`, `WEB-FRESH-001`, `WEB-CLAIM-001`, `WEB-VERSION-001`, and `BASE-REL-013..014`.
- Protected domains: graph/audit/snapshot schema 2; config schema 2 with schema-1 compatibility; cache schema 1; the exact five-file snapshot contract; thirty rules and severities; semantics of the seven existing analysis commands; project manifest schema 1; immutable 0.4.0 and 0.5.0 artifacts; provider independence; indexed-only agent conclusions; and the existing website design, narrative, interactions, accessibility, deployment mechanics, and visual assets except the user-authorized removal of the version chip from the social preview.
- Shared owners: specification registry, release baseline, public docs, release-fact website strings, tagged `skills/`, GitHub Actions, the local versioned skill bundle, and `potapenko/homebrew-tap`.
- Authority/stability: Active release evolution at epoch `tz-v20`; 0.5.0 remains the current public release and 0.6.0 remains a publication-authorized candidate until the terminal publication receipt is recorded.
- Required evidence: accepted project-watcher implementation and local acceptance; hosted candidate CI `32624693470`; locked build/tests and dogfood; exact CLI option-ownership checks; four skill validations; tag CI; archive SHA-256 and clean extraction build; formula style/audit/source install/test; hosted tap source install; local CLI/skill verification; and public-site release-fact verification.
- Allowed delta: released/candidate wording, 0.6.0 links and version output, release notes, archive checksum, formula URL/checksum/test version, tagged local skill targets, project-namespace documentation, removal of the social-preview version chip with updated provenance/hash, and exact publication receipts.
- Forbidden delta: graph/config/cache/snapshot schema changes; rule, severity, or existing analysis-command semantic changes; project schema changes; automatic rewriting; bottles; notarization; Homebrew writes to agent-host directories; automatic skill installation; website redesign; retagging or republishing 0.4.0/0.5.0; or moving any published tag.
- Stop conditions: remote divergence; a pre-existing conflicting 0.6.0 tag, release, or asset; failed candidate or tag CI; checksum mismatch; failed source build or brew test; formula/skill ownership violation; or non-terminal public verification.
- Pinned basis: registry `spec-23`, combined epoch `tz-v20`, original authority digest unchanged; watcher implementation `c392ed284fd4af849c7d6153eb2510b3d7efea2c`; diff-fallback repair `4342c269b53842fad279ed6bd6f3e5f940db6ea9`; skill flag-ownership clarification `eea2bdff11f0d2db76ff51668c55477a7f24e1f9`; persisted-slice parser guard `91860a1ac132b5941b5ee8527d7da64c7f103815`; accepted candidate head and indexed baseline `9c8d5475fd6bace8d9ff607bd76250dcbfd1ff70`; hosted candidate CI `32624693470`.
- Task-owned release paths: the approved release/spec/public/website paths in this repository, the social-preview version chip and provenance, the upstream tap formula and its test workflow, and the separately versioned local skill bundle; no Swift product-source paths are opened by this release envelope.

## Contract Delta

- Previous behavior: 0.5.0 was the current public GitHub/Homebrew/tagged-skill release; 0.6.0 was an accepted local project-watcher candidate whose later flag guidance, diff fallback, parser guard, and indexed baseline had not been published.
- New behavior: 0.6.0 is authorized for immutable GitHub and source-built Homebrew publication with the same separately installed four-skill boundary and factual public release updates. It becomes Released only after the terminal receipt pins every required external artifact and verification result.
- Compatibility: additive minor release. Project manifest schema 1 and the project namespace are added to the distribution while graph/config/cache/snapshot schemas, thirty rules, the seven existing analysis-command semantics, indexed agent gates, and immutable 0.4.0/0.5.0 artifacts remain unchanged.
- Evidence basis: `PROJECT-WATCHER-001`, local candidate acceptance, `c392ed2`, `4342c26`, `eea2bdf`, `91860a1`, accepted head `9c8d547`, hosted candidate CI `32624693470`, and the terminal publication receipt added only after external verification.
- Adjacent domains checked: Homebrew remains CLI-only; tagged skills remain separate and operator-owned; no bottles are claimed; website changes are release facts plus one user-authorized version-chip removal and do not otherwise alter design or interaction behavior.
- Specification revision: combined epoch `tz-v20`, local registry `spec-24`.
